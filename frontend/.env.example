
# Leave unset for `npm run dev`: requests use same-origin `/api` (Vite proxy) so login cookies work.
# Only set a full URL if the SPA is hosted on a different origin than the API.
# VITE_API_URL=http://localhost:5000/api
